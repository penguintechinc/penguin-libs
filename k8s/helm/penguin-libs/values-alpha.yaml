namespace: penguin-libs-alpha
buildValidator:
  image:
    pullPolicy: Never
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "256Mi"
      cpu: "250m"
config:
  nodeEnv: "test"
