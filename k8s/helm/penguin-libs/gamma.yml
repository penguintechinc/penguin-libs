buildValidator:
  image:
    tag: "20-alpine"
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "250m"

config:
  nodeEnv: "gamma"
  npmRegistry: "https://registry.npmjs.org"
