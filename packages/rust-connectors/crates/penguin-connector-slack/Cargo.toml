[package]
name = "penguin-connector-slack"
description = "Slack connector: Socket Mode receive, chat.postMessage send (scaffold — see crate docs)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
keywords = ["slack", "socket-mode", "connector"]
categories = ["network-programming", "asynchronous"]

[dependencies]
thiserror = "=2.0.20"

[lints.rust]
unsafe_code = "deny"
missing_docs = "deny"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
