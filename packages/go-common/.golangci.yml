version: "2"
linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gosec
    - bodyclose
    - noctx
    - exhaustive
  settings:
    exhaustive:
      default-signifies-exhaustive: true
run:
  timeout: 5m
